// Number Mirror
// Write a program that takes a number 
// �
// N as the input, and prints it to the output.
#include <stdio.h>
int main() {
    c = a + b;
    scanf("%d", &a);
    scanf("%d", &b);
    printf("%d", c);
    int a,b,c;
    return 0;
}